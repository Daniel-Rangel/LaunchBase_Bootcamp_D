{% extends "layout.njk"%}

{% block head %}
  <title>Sobre - Rocketseat </title>
{% endblock %}

{% block content %}
  
  <div class="card card-table">
    <a href="http:/students/create" class="btnCriar">Novo</a>
    <table>
      <thead>
        <tr>
          <th>Nome completo</th>
          <th>Email</th>
          <th>Ano letivo</th>
          <th>Ação</th>
        </tr>
      </thead>
      <tbody>
        {% for student in students %}
        <tr>
          <td>
            <span style="background-image: url({{student.avatar_url}})"></span>
            {{student.name}}
          </td>
          <td>
            {% for subjects in student.school_subjects.split(',') %}
              <span>{{subjects}}</span>
            {% endfor %}
          </td>
          <td><a href="/students/{{student.id}}">Ver</a></td>
        </tr>
        {% endfor %}

      </tbody>
    </table>
  </div>

{% endblock %}
